export interface SocialItem {
  icon: any;
  title: string;
  handle: string;
  link: string;
  bgColor: string;
  textColor: string;
  buttonText: string;
  buttonBg: string;
  backgroundImage?: string;
}
